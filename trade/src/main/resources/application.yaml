ktor:
  application:
    modules:
      - dmitriy.losev.cs.ApplicationKt.module
  deployment:
    port: 8080
  database:
    url: ${DB_URL}
    user: ${DB_USER}
    password: ${DB_PASSWORD}
  crypto:
    aesKey: ${AES_KEY}
  csfloat:
    url: "http://csfloat:80"
  pulse:
    email: ${PULSE_EMAIL}
    password: ${PULSE_PASSWORD}
  app:
    environment: "prod"
    debug: true
  proxy:
    host: ${MOBILE_PROXY_HOST}
    devices:
      - deviceId: "e3e65dbd"
        port: 1070
        deviceName: "Xiaomi Mi 6"
        login: "VEVdfm66"
        password: "5aDce5F5"
      - deviceId: "82bc3269"
        port: 1069
        deviceName: "Xiaomi Mi 6"
        login: "Hp0lkI7B"
        password: "mLXIZc2y"
      - deviceId: "4e5a1437"
        port: 1068
        deviceName: "Xiaomi Mi 8 Lite"
        login: "BNYz6k7d"
        password: "Fr8hkYw9"
  logging:
    http:
      enabled: true
      level: DEBUG
      excludedPaths:
        - /health
        - /metrics
        - /favicon.ico
      sensitiveFields:
        - password
        - secret
        - token
        - apiKey
        - api_key
        - identity_secret
        - shared_secret
        - revocation_code
      sensitiveHeaders:
        - authorization
        - cookie
        - x-api-key
      maxBodySize: 10000  # обрезать body если больше N символов
